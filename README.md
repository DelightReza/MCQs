# QuizDroid

Universal Quiz Platform built with pure HTML, CSS, and Vanilla JavaScript.

## Run locally

Open `index.html` directly or serve the folder as static files.

## Add a new question bank

1. Add a TXT file to `/questionbanks`
2. Add its entry to `metadata.json`

No code changes required.
